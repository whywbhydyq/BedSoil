const currentSha=process.env.VERCEL_GIT_COMMIT_SHA,ref=process.env.VERCEL_GIT_COMMIT_REF,owner=process.env.VERCEL_GIT_REPO_OWNER,repo=process.env.VERCEL_GIT_REPO_SLUG;
if(!currentSha||!ref||!owner||!repo){console.log('Missing Vercel Git environment variables. Continue build.');process.exit(1)}
try{const headers={Accept:'application/vnd.github+json','User-Agent':'skip-old-vercel-builds'};if(process.env.GITHUB_TOKEN)headers.Authorization=`Bearer ${process.env.GITHUB_TOKEN}`;const res=await fetch(`https://api.github.com/repos/${owner}/${repo}/commits/${encodeURIComponent(ref)}`,{headers});if(!res.ok){console.log(`GitHub API check failed with ${res.status}. Continue build.`);process.exit(1)}const data=await res.json();if(data?.sha&&data.sha!==currentSha){console.log(`Skip old Vercel build. Current: ${currentSha}. Latest: ${data.sha}.`);process.exit(0)}console.log('Current commit is latest. Continue build.');process.exit(1)}catch(e){console.log('Skip-old-build check failed. Continue build.',e);process.exit(1)}
